[package]
name = "remi-client-sdk"
version = "0.1.0"
edition = "2024"
resolver = "2"

[dependencies]
remi-things-crdt = { path = "../remi-things-crdt" }
rule-trigger-engine = { path = "../rule-trigger-engine" }
anyhow = "1.0"
base64 = "0.22"
chrono = { version = "0.4", default-features = false, features = ["clock", "serde", "std"] }
croner = "3.0.1"
futures = "0.3"
async-stream = "0.3"
http = "0.2"
once_cell = "1.19"
prost = "0.14"
prost-types = "0.14"
rusqlite = { version = "0.31", features = ["bundled", "chrono", "serde_json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
jsonschema = "0.18"
thiserror = "1.0"
tokio = { version = "1.37", features = ["macros", "rt-multi-thread", "sync", "time", "net", "fs"] }
tokio-tungstenite = { version = "0.26", features = ["rustls-tls-webpki-roots"] }
tokio-stream = "0.1"
tokio-util = { version = "0.7", features = ["rt"] }
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
url = "2"
urlencoding = "2.1"
tonic = { version = "0.14", features = ["transport"] }
tonic-prost = "0.14"
tracing = "0.1"
automerge = "0.7.2"
sentry = { version = "0.35", default-features = false, features = ["backtrace", "contexts", "reqwest", "rustls"], optional = true }
uuid = { version = "1.10", features = ["v4", "v5"] }
async-trait = "0.1"
rquickjs-core = { version = "0.8.1", default-features = false, features = ["bindgen", "rust-alloc"], optional = true }

[features]
default = []
sentry-integration = ["sentry"]
quickjs = ["dep:rquickjs-core"]

[build-dependencies]
protoc-bin-vendored = "3.0"
tonic-prost-build = "0.14"

[dev-dependencies]
tempfile = "3"
serial_test = "3"
criterion = { version = "0.5", features = ["html_reports"] }
