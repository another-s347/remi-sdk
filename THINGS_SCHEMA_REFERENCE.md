# Things Schema Reference

The current Things schema covers root collection indexes, collection metadata, thing metadata, markdown content, JSON object entry data, JSON object entry schemas, tombstones, status, actor metadata, and timestamps.

Legacy automation binding properties from old documents are not part of the current schema. Readers should ignore unknown fields; writers must not emit them.
